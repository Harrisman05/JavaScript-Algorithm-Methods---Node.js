// Split divides a String into an order list of substrings, puts these substrings into an array, and returns the array
// The division is done by searching for a pattern, where the pattern is provided as the first paramater in the method's call

const str = 'The quick brown fox jumps over the lazy dog.';

let splitStr = str.split(' ');

console.log(splitStr[3]);

