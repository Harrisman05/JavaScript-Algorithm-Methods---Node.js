// forEach method

// Allows you to apply a function to each element in an array. It doesn't return anything so inputs aren't saved into new array.

const arr = [1,2,3,4,5];

arr.forEach((element, i) => {
    console.log(`I am element ${element} at index ${i}`);
});

